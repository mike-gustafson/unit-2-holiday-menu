<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lost Password</title>
    <%- include('../../partials/header.ejs') %>
</head>

<body>
    <div class="container mx-auto">
        <div class="card lost-password rounded row">
        <div class="col s8">
        </div>
        <div class="col s4 opacity-50-dark">
            <h1>Lost your password?</h1>
            <p>Enter your email address and we will send you a link to reset your password.</p>
            <br>
            <div class="container">
              <% if (error.length> 0) { %>
                <div>
                  <%= error[0] %>
                </div>
                <% } %>
          
                  <% if (success.length> 0) { %>
                    <div>
                      <%= success[0] %>
                    </div>
                    <% } %>
            </div>
            <div>
                <form action="/password" method="POST" class="space-y-4">
                    <div class="row">
                        <div class="input-field col s8 offset-s1">
                            <input type="email" name="email" id="email" required />
                            <label for="email">E-Mail</label>
                        </div>
                    </div>
                    <div class="col s2"></div>
                    <div class="col s12">
                        <button type="submit" class="waves-effect waves-light btn green">Send Reset Email</button>
                        <hr>
                    </div>
                </form>
                <div class="col s12">
                    <a href="/login">Login</a>
                    <span>|</span>
                    <a href="/register">Register</a>
                </div>
            </div>
        </div>  
      </div>
    </body>
</body>
